# pytest.ini
[pytest]
testpaths = tests
addopts = --import-mode=importlib
markers =
    main_generator: guards src/device/generate.py (the combinatorial device-kernel generator)
    symmetric_generator: guards src/device/symmetric/generate.py
    diagnostics: unit tests for the diff/report helper
