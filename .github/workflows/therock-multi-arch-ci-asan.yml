# Copyright Advanced Micro Devices, Inc.
# SPDX-License-Identifier: MIT

# Multi-Arch CI ASAN for rocm-systems
#
# Presubmit and on-demand ASAN builds. TheRock's configure_multi_arch_ci.py
# automatically selects host-asan for pull_request events (faster, host-only)
# and full asan for workflow_dispatch (comprehensive device-side instrumentation).
#
# The presubmit is scoped to a runtime-only build (compiler-runtime and
# runtime-tests) testing hip-tests and rocrtst, so that it is cheap enough to
# run automatically on every pull request. See ROCm/TheRock#7202.
#
# For scheduled nightly runs, see therock-multi-arch-ci-asan-nightly.yml.

name: TheRock Multi-Arch CI ASAN

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - labeled
  workflow_dispatch:
    inputs:
      linux_amdgpu_families:
        type: string
        description: "Insert comma-separated list of Linux GPU families to build and test. ex: gfx94X, gfx120X"
        default: ""
      linux_test_labels:
        type: string
        description: "If enabled, reduce test set on Linux to the list of labels prefixed with 'test:'. ex: test:rocprim, test:hipcub"
        default: ""
      prebuilt_stages:
        type: string
        default: ""
        description: "Comma-separated build stages to skip (or 'all' for all stages); artifacts are copied from baseline_run_id instead"
      baseline_run_id:
        type: string
        default: ""
        description: "Workflow run ID to copy prebuilt stage artifacts from; required when prebuilt_stages is set"

permissions:
  contents: read
  actions: read

concurrency:
  # Only allow one run at a time for 'pull_request' events.
  # Allow multiple runs for 'push' and 'workflow_dispatch'.
  #
  # `github.workflow`: each workflow gets its own concurrency group
  # `github.head_ref`: the branch name (only defined for pull_request events)
  # `github.run_id`  : fallback, unique for each run
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  setup:
    # TEMPORARY: pinned to the TheRock branch under review in
    # https://github.com/ROCm/TheRock/pull/7311. Both the `uses:` ref and the
    # `ref:` input below must point at the same branch: `uses:` selects the
    # workflow_call interface, while `ref:` selects the TheRock checkout that
    # actually runs configure_multi_arch_ci.py. Restore both to `main` once
    # that PR merges.
    uses: ROCm/TheRock/.github/workflows/setup_multi_arch.yml@users/dravindr/host-asan-presubmit
    with:
      build_variant: "asan"
      # Only applies to workflow_dispatch; a pull_request trigger starts from
      # TheRock's presubmit family defaults and drops every family that has no
      # host-asan variant, which leaves gfx94X either way.
      linux_amdgpu_families: ${{ inputs.linux_amdgpu_families || 'gfx94X' }}
      linux_test_labels: ${{ inputs.linux_test_labels || 'test:hip-tests,test:rocrtst' }}
      # Runtime-only build: everything outside these two stages, math-libs
      # included, is dropped from the build graph.
      build_stages: "compiler-runtime,runtime-tests"
      # Run on every PR with no ci:asan / ci:host-asan label, and allocate an
      # ASAN sandbox test runner on the pull_request trigger.
      asan_presubmit: true
      # PR test:* labels must not widen the pinned test scope.
      lock_test_labels: true
      # The sanity check currently reports a LeakSanitizer failure originating
      # in libamdhip64, which would otherwise hide every hip-tests and rocrtst
      # result behind it. Sanity still runs and still reports its own status.
      sanity_blocks_components: false
      # Post-processing that a partial build cannot satisfy.
      validate_artifact_structure: false
      build_native_linux: false
      build_python_packages: false
      build_pytorch: false
      build_jax: false
      prebuilt_stages: ${{ inputs.prebuilt_stages || '' }}
      baseline_run_id: ${{ inputs.baseline_run_id || '' }}
      repository: ROCm/TheRock
      ref: "users/dravindr/host-asan-presubmit"
      external_repo: '{"repository":"${{ github.repository }}","ref":"${{ github.sha }}"}'

  linux_build_and_test:
    name: Linux::${{ fromJSON(needs.setup.outputs.linux_build_config || '{}').build_variant_label || 'skip' }}
    needs: setup
    if: >-
      ${{
        needs.setup.outputs.linux_build_config != '' &&
        needs.setup.outputs.enable_build_jobs == 'true'
      }}
    # TEMPORARY: see the note on the setup job. Restore to `main` once
    # https://github.com/ROCm/TheRock/pull/7311 merges. The `ref:` input below
    # already follows setup's resolved commit, so it needs no change.
    uses: ROCm/TheRock/.github/workflows/multi_arch_ci_linux.yml@users/dravindr/host-asan-presubmit
    with:
      build_config: ${{ needs.setup.outputs.linux_build_config }}
      test_labels: ${{ needs.setup.outputs.linux_test_labels }}
      rocm_package_version: ${{ needs.setup.outputs.rocm_package_version }}
      test_type: ${{ needs.setup.outputs.test_type }}
      external_repo_config: ${{ needs.setup.outputs.external_repo_config }}
      repository: ROCm/TheRock
      ref: ${{ needs.setup.outputs.ref }}
    permissions:
      contents: read
      id-token: write

  multi_arch_ci_summary:
    name: Multi-Arch CI Summary
    # always() is needed to show the job statuses regardless of failure
    if: always()
    needs:
      - setup
      - linux_build_and_test
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout TheRock repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          repository: "ROCm/TheRock"
          ref: ${{ needs.setup.outputs.ref }}
          sparse-checkout: build_tools/github_actions
          sparse-checkout-cone-mode: true

      - name: Evaluate workflow results
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          python build_tools/github_actions/workflow_summary.py \
            --needs-json '${{ toJSON(needs) }}'
