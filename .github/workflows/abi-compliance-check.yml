name: ABI Compliance Check

on:
  pull_request:
    branches:
      - develop
    paths:
      - 'projects/amdsmi/**'
  push:
    branches:
      - develop
    paths:
      - 'projects/amdsmi/**'
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  major_abi_check:
    name: Major ABI Compliance Check
    runs-on: ubuntu-22.04
    steps:
      - name: Setup Environment
        run: |
          sudo rm -rf $GITHUB_WORKSPACE/* || true
          sudo rm -rf $GITHUB_WORKSPACE/.[!.]* || true
          sudo apt-get update -qq
          sudo apt-get install -y -qq perl build-essential git universal-ctags
          git clone https://github.com/lvc/abi-compliance-checker.git
          cd abi-compliance-checker
          sudo make install
          abi-compliance-checker --version

      - name: Checkout current code (new version)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Fetch base branch for PR
        if: github.event_name == 'pull_request'
        env:
          BASE_REF: ${{ github.base_ref }}
        run: |
          echo "Fetching base branch: $BASE_REF"
          git fetch origin -- "$BASE_REF:$BASE_REF"
          git branch -a

      - name: Determine Base Ref
        id: base_ref
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
          EVENT_BEFORE: ${{ github.event.before }}
        run: |
          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            echo "ref=$BASE_REF" >> $GITHUB_OUTPUT
          elif [[ "$EVENT_NAME" == "push" && "$EVENT_BEFORE" != "0000000000000000000000000000000000000000" ]]; then
            echo "ref=$EVENT_BEFORE" >> $GITHUB_OUTPUT
          else
            echo "ref=" >> $GITHUB_OUTPUT
          fi

      - name: Run Major ABI Compliance Check
        id: run_major
        if: steps.base_ref.outputs.ref != ''
        env:
          BASE_REF: ${{ steps.base_ref.outputs.ref }}
          HEAD_REF: ${{ github.head_ref || github.ref_name }}
        run: |
          python3 projects/amdsmi/tests/abi_check/abi_check.py \
            --base-ref "$BASE_REF" \
            --head-ref "$HEAD_REF" \
            --project-path projects/amdsmi \
            --mode major
        continue-on-error: true

      - name: Label PR on Major ABI Breakage
        if: always() && github.event_name == 'pull_request'
        run: |
          if [[ "$RUN_MAJOR_OUTCOME" == "failure" ]]; then
            echo "Major ABI check failed, adding MAJOR ABI BREAKAGE label to PR #${PR_NUMBER}"
            gh pr edit "$PR_NUMBER" --add-label "MAJOR ABI BREAKAGE"
          else
            echo "Major ABI check passed, removing MAJOR ABI BREAKAGE label if present"
            gh pr edit "$PR_NUMBER" --remove-label "MAJOR ABI BREAKAGE" || true
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_MAJOR_OUTCOME: ${{ steps.run_major.outcome }}
          PR_NUMBER: ${{ github.event.pull_request.number }}

      - name: Upload Major ABI Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: major-abi-report
          path: abi_report.html
          if-no-files-found: warn

      - name: Report Major ABI Check Results
        if: always()
        env:
          RUN_MAJOR_OUTCOME: ${{ steps.run_major.outcome }}
        run: |
          if [[ "$RUN_MAJOR_OUTCOME" == "failure" ]]; then
            echo "::warning::MAJOR ABI BREAKAGE FOUND. Check Run Major ABI Compliance Check logs or the major-abi-report artifact."
          elif [[ "$RUN_MAJOR_OUTCOME" == "success" ]]; then
            echo "Major ABI check succeeded."
          else
            echo "Major ABI check was skipped (no baseline ref available)."
          fi

  minor_abi_check:
    name: Minor ABI Compliance Check
    needs: major_abi_check
    runs-on: ubuntu-22.04
    steps:
      - name: Setup Environment
        run: |
          sudo rm -rf $GITHUB_WORKSPACE/* || true
          sudo rm -rf $GITHUB_WORKSPACE/.[!.]* || true
          sudo apt-get update -qq
          sudo apt-get install -y -qq perl build-essential git universal-ctags
          git clone https://github.com/lvc/abi-compliance-checker.git
          cd abi-compliance-checker
          sudo make install
          abi-compliance-checker --version

      - name: Checkout current code (new version)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Fetch base branch for PR
        if: github.event_name == 'pull_request'
        env:
          BASE_REF: ${{ github.base_ref }}
        run: |
          echo "Fetching base branch: $BASE_REF"
          git fetch origin -- "$BASE_REF:$BASE_REF"
          git branch -a

      - name: Determine Base Ref
        id: base_ref_minor
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
          EVENT_BEFORE: ${{ github.event.before }}
        run: |
          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            echo "ref=$BASE_REF" >> $GITHUB_OUTPUT
          elif [[ "$EVENT_NAME" == "push" && "$EVENT_BEFORE" != "0000000000000000000000000000000000000000" ]]; then
            echo "ref=$EVENT_BEFORE" >> $GITHUB_OUTPUT
          else
            echo "ref=" >> $GITHUB_OUTPUT
          fi

      - name: Run Minor ABI Compliance Check (Strict)
        id: run_minor
        if: steps.base_ref_minor.outputs.ref != ''
        env:
          BASE_REF: ${{ steps.base_ref_minor.outputs.ref }}
          HEAD_REF: ${{ github.head_ref || github.ref_name }}
        run: |
          python3 projects/amdsmi/tests/abi_check/abi_check.py \
            --base-ref "$BASE_REF" \
            --head-ref "$HEAD_REF" \
            --project-path projects/amdsmi \
            --mode minor
        continue-on-error: true

      - name: Label PR on Minor ABI Breakage
        if: always() && github.event_name == 'pull_request'
        run: |
          if [[ "$RUN_MINOR_OUTCOME" == "failure" ]]; then
            echo "Minor ABI check failed, adding MINOR ABI BREAKAGE label to PR #${PR_NUMBER}"
            gh pr edit "$PR_NUMBER" --add-label "MINOR ABI BREAKAGE"
          else
            echo "Minor ABI check passed, removing MINOR ABI BREAKAGE label if present"
            gh pr edit "$PR_NUMBER" --remove-label "MINOR ABI BREAKAGE" || true
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_MINOR_OUTCOME: ${{ steps.run_minor.outcome }}
          PR_NUMBER: ${{ github.event.pull_request.number }}

      - name: Upload Minor ABI Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: minor-abi-report
          path: abi_report.html
          if-no-files-found: warn

      - name: Report Minor ABI Check Results
        if: always()
        env:
          RUN_MINOR_OUTCOME: ${{ steps.run_minor.outcome }}
        run: |
          if [[ "$RUN_MINOR_OUTCOME" == "failure" ]]; then
            echo "::warning::MINOR ABI CHANGES FOUND (STRICT CHECK). Check Run Minor ABI Compliance Check logs or the minor-abi-report artifact."
          elif [[ "$RUN_MINOR_OUTCOME" == "success" ]]; then
            echo "Minor ABI check (Strict) succeeded."
          else
            echo "Minor ABI check was skipped (no baseline ref available)."
          fi
